[build]
builder = "NIXPACKS"
buildCommand = "cd frontend && node node_modules/vite/bin/vite.js build && cd ../backend && npm install"

[deploy]
startCommand = "cd backend && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
