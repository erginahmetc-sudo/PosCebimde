[build]
builder = "NIXPACKS"
buildCommand = "cd /app/frontend && npm install && npm run build && cd /app/backend && npm install"

[deploy]
startCommand = "cd backend && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
